# A 方案图标落地

## 目标与决定

用户在本任务中批准最后展示的 A 单图。保持四条金色光带、偏心细汇聚点和
光带在边缘被截断的构图，不能沿用中途被否决的改形精修稿。

基线为 `03e9c91`，工作区初始干净。本次更新当前母版、各尺寸图标和网页外壳
缓存；不改变编辑、存储逻辑，也不把批准图稿扩大为安装或远程发布授权。

## 当前状态

- 最后批准稿原样保存为 `docs/brand/approved-a.png`。
- 批准稿 SHA-256 为 `ccc237add406b170764af425d0baf339e36d13914f683d471f9a0b150259cd49`，
  与用户最后确认的生成文件逐字节一致。
- 母版只移除四角的白色展示底并处理其边缘透明度；缩放前检查四角区域之外
  的全部 RGB 与不透明度，变化像素为 0。随后等比缩放到 916 × 916，并在
  1024 × 1024 画布各边留出 54px，与此前桌面图标的占位一致。
- 图标说明、桌面 ICNS/PNG/ICO、网页 favicon、touch icon、PWA 与已有其他平台
  导出资源已更新。当前不因此声明支持或验证其他平台运行。
- 网页缓存由 v6 更新为 v7；没有改变其他运行逻辑。

## 实际验证

- `npm run build:icons`：PASS，使用现有 Tauri 导出流程。
- `npm test -- src/pwa/serviceWorker.test.ts`：6 项通过；发布卫生检查通过。
- `npm run check:desktop-runtime`：Web/MCP 构建、隔离桌面构建和启动通过；
  打包 ICNS 与源导出 SHA-256 一致，严格本地签名校验通过。
- macOS `NSWorkspace` 从新构建的 `.app` 读取到 A 图标，已查看实际渲染。
  系统会添加应用图标的外部呈现效果，该效果不写入母版。
- 浏览器实际展示 16、24、32、48、64、128px 导出，核对图片原生像素和显示像素
  一致，查看浅色、深色背景。四条光带、细汇聚点、边缘截断保持批准的构图；
  16px 下细部有限，24/32px 的主光带间隙可见，没有改形补偿。
- 在隔离浏览器中先安装真实 v6 worker 与旧图标，再更新到本次 v7。等待新
  worker 激活并接管后，v6 缓存删除，六个图标的响应摘要均与新构建一致且
  不同于旧版；离线重新加载编辑器与读取图标通过，页面无脚本错误。
  初次检查在新 worker 安装尚未完成时取样，读到了旧资源；临时验证脚本改为
  等待真实激活与接管事件后再比较，未为此修改产品逻辑或放宽摘要断言。

PWA 测试中的版本期望值随新的图标资源变为 v7，依据是本次用户批准的替换；
上述浏览器升级和离线流程提供了断言之外的实际证据。

## 交付边界

本次图标与缓存范围已完成；未运行无关的编辑、保存与快捷键流程，不把隔离
启动扩张为这些流程已验证。没有替换 `/Applications/Laniakea.app`，原进程
89392 和旧 ICNS 摘要在本次前后保持一致。未提交、推送、发布网站或更新插件。
后续若要更新本机安装，应在授权后用本次源码完成正式应用构建和替换。

临时处理脚本、记录和截图：`/tmp/laniakea-icon-a-20260907/`。
