# 2026-09-07 图标落地与本机更新

用户批准金色星系流线提案，并授权后续处理。基线为 `b7c5148`，工作区初始干净。

## 改动

- 批准稿与 PNG 母版保存在 `docs/brand/`；保留批准图像的光束形状和配色，
  只处理展示背景、透明边缘、方形比例与导出尺寸。
- `npm run build:icons` 使用现有 Tauri CLI 生成图标；桌面 ICNS、网页 favicon、
  touch icon 与 PWA 资源共用母版。移除旧圆点 SVG，单列不透明 maskable 资源。
- 网页应用外壳缓存从 v5 更新至 v6，预缓存新的图标路径。
- 本机安装版本更新为 0.3.4；应用标识仍为 `com.openadam.origin`。

## 本次验证

- 桌面正式构建、严格本地签名校验通过；包内 ICNS 与源码导出摘要一致。
- 安装前应用已退出。备份旧 .app 与完整应用数据后替换；完整文件清单验证
  新安装包与构建结果一致。
- 实际启动 `/Applications/Laniakea.app`，原有 `agent办公培训` 文档恢复。
  Finder 简介实际显示新图标与 0.3.4；核对 32 像素导出和系统预览的可辨认性。
  从原文档的流程入口进入后，4 个步骤与 3 条连线正常显示。
- 安装后核对 88 份 Markdown（包含草稿与历史备份）内容一致，活动文档绑定
  和偏好设置未改变。
- 构建后的网页正常打开，DOM 图标引用正确，控制台无警告或错误；停止本地
  服务器后仍能重新加载应用外壳。此检查不等同于已安装 PWA 的操作系统图标更新验证。
- PWA 相关 6 项测试、版本与发布卫生检查、MCP 真实传输回归通过。
  测试中的缓存版本期望随本次图标更新变为 v6；通过依据还包括实际离线重载。
  本次不修改编辑或存储逻辑，不重复此前同一实现已通过的全部编辑回归。

本地截图与日志：`/tmp/laniakea-brand-20260907/`。旧包与数据备份保存在用户
Application Support 下的 OpenAdam/App Backups/Laniakea，具体路径见安装记录。

未推送或发布远程网站，未更新已安装的 Agent Host 插件。本机 .app 仍是本地签名；
面向公众的 DMG 需要 Apple Developer 身份、正式签名与公证后另行发布。
