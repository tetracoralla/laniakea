# Laniakea 仓库指引

修改相关行为前，读 `docs/product-model.md` 和 `docs/agent-tool-model.md`，
再按受影响的风险查看 `docs/REVIEW_CONTRACT.md`。文档描述当前边界；本次用户已
授权的功能和修复可以推动它演进，不需要让现有实现反过来否决需求。

是否启动独立 review、何时启动、使用哪个 reviewer，由用户决定并手动调度。
施工 Agent 负责自己的源码检查、回归和相关真实流程，不自动调用子智能体、
审查命令、替代模型或复审。若用户直接要求本 Agent 审查，就在所要求的范围内
只读执行；同时要求修复时，记录初始发现、验证后修复并检查受影响行为。
契约是已知风险线索，不能限制合理探索，也不能把局部复核扩大为所有载体审计。
发现跨仓库问题时具体说明；无需为了报告格式填写空的工作区结论。

## 界面取舍

画布、导航和交互文案沿用 `agent-ux-ui-execution` 的方法；本项目的具体取舍见
`docs/product-model.md`。从用户刚完成的动作、正在操作的对象和下一步任务判断，
优先就地继续，减少阅读、寻找和注意力转移。新增常驻元素要解决具体缺口；
删掉解释后，由可见线索、即时反馈和往返状态承接理解，不能变成隐藏操作或猜测。
已有熟悉入口保持稳定，不因推测用户熟练度而自动移动或隐藏。

## 验证与验收分工

- `npm run check:regression` 是开发回归检查。它只回答当前代码是否通过既有
  单元、组件、构建和 Rust 检查，不能宣布产品、业务、视觉或体验验收通过。
- `npm run check:desktop-runtime` 构建并启动隔离 `.app`，脚本成功只证明启动。
  涉及 Tauri、文件、窗口、快捷键、剪贴板或打包行为时，Agent 还应在该新构建上
  实际操作受影响流程，分别报告启动与流程的 `PASS / FAIL / BLOCKED`；不要把
  技术判断转交给用户肉测。
- Agent 应自行判断视觉、可用性和任务流程，修复明确问题。用户保留最终业务与
  体验验收；这不免除 Agent 的产品判断，也不要求用户承担技术测试。
- 报告本次实际验证的范围和缺口；涉及多个载体时分别说明。测试数量、覆盖率、
  截图或报告不能替代产品判断，未运行桌面流程就不声称桌面已验证。

## 连续状态覆盖

- 涉及保存、切换、异步操作、模式、选择、手势或撤销的改动，测试真实顺序和
  组合状态。`A` 单独通过、`B` 单独通过，不代表 `A -> B` 或两者同时发生时
  正确。
- 回归测试要从用户入口触发，检查跨步骤遗留状态、目标文档、持久化对象和最终
  可见结果。纯函数或 mock 测试可以保留，但只能声明其实际覆盖的边界。
- 同一任务内若 Agent 同时修改需求文档、断言、fixture、snapshot、期望值或
  检查脚本，绿灯只是候选结果；必须说明期望值来自现有产品要求、真实运行状态
  还是本次明确的用户决定。
