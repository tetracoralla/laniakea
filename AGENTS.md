# 原点仓库指引

## 验证与验收分工

- `npm run check:regression` 是开发回归检查。它只回答当前代码是否通过既有
  单元、组件、构建和 Rust 检查，不能宣布产品、业务、视觉或体验验收通过。
- `npm run check:desktop-runtime` 是桌面运行检查。涉及 Tauri、文件、窗口、
  快捷键、剪贴板或打包行为时，由 Agent 在新构建的 `.app` 上执行并报告
  `PASS / FAIL / BLOCKED`；不要把技术判断转交给用户肉测。
- 用户只负责业务与体验意见：视觉是否合适、操作是否顺手、任务是否成立，
  结论使用 `OK / 不OK / 待验收`。技术检查全绿不能覆盖用户的体验结论。
- 最终报告分别列出“开发回归”“桌面真实流程”“体验验收”。不要生成一个
  合并的“全部绿灯”或用测试数量、覆盖率、截图、报告代替产品判断。

## 连续状态覆盖

- 涉及保存、切换、异步操作、模式、选择、手势或撤销的改动，测试真实顺序和
  组合状态。`A` 单独通过、`B` 单独通过，不代表 `A -> B` 或两者同时发生时
  正确。
- 回归测试要从用户入口触发，检查跨步骤遗留状态、目标文档、持久化对象和最终
  可见结果。纯函数或 mock 测试可以保留，但只能声明其实际覆盖的边界。
- 同一任务内若 Agent 同时修改需求文档、断言、fixture、snapshot、期望值或
  检查脚本，绿灯只是候选结果；必须说明期望值来自现有产品要求、真实运行状态
  还是本次明确的用户决定。

