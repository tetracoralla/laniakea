# Contributing to Laniakea

感谢你愿意改进 Laniakea。

## 开始

1. Fork 仓库并从 `main` 创建分支。
2. 运行 `npm install`。
3. 使用 `npm run dev` 开发网页界面，或使用 `npm run desktop:dev` 开发桌面版。
4. 提交前运行 `npm run check:regression`。

## 变更原则

- 保持核心体验快速、轻量，避免把设置、诊断或实现细节放到主要画布上。
- 网页版和桌面版共享文档与编辑模型，但只显示当前平台真正支持的操作。
- 修改保存、切换、导入或异步流程时，请覆盖完整的连续操作顺序，而不只测试单个函数。
- 修复缺陷时加入一个能够防止同类问题回归的最小测试。
- 不要提交真实用户文档、访问令牌、签名证书或其他敏感信息。

较大的行为变化请先开 Issue 说明用户问题、建议交互和兼容性影响。
